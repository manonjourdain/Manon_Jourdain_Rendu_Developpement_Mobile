<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rendumineurtech">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".coconut_pie"></activity>
        <activity android:name=".vegan" />
        <activity android:name=".fil_actualite" />
        <activity android:name=".Welcome" />
        <activity android:name=".Onboarding3" />
        <activity android:name=".Onboarding2" />
        <activity android:name=".Onboarding1" />
        <activity android:name=".activity_view_list_item" />
        <activity android:name=".ItemEditActivity" />
        <activity android:name=".ItemsAdapter" />
        <activity android:name=".listItemActivity" />
        <activity android:name=".addItemActivity" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>